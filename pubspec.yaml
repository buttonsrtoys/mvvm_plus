name: mvvm_plus
description: MVVM+ is a lightweight Flutter state management library that implements MVVM, plus adds support for sharing business logic across widgets.
version: 0.2.3
repository: https://github.com/buttonsrtoys/mvvm_plus
issue_tracker: https://github.com/buttonsrtoys/mvvm_plus/issues

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  registrar: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true